import React from "react";
import { Grid } from "@nextui-org/react";

type Props = {};

const index = (props: Props) => {
  return (
    <Grid.Container
      css={{
        width: "100%",
        height: "60vh",
        pl: "$8",
        pr: "$8",
      }}
    >
      algorithm
    </Grid.Container>
  );
};

export default index;
